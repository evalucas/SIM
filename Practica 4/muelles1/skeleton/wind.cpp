#include "wind.hpp"
#include"Particle.hpp"

//void ParticleWind::updateForce(Particle* particle, float t) {
//	PX_UNUSED(t);
//	Vector3 pp = particle->getPosition();
//	float distance = (pp - origin).magnitude();
//	if (distance < radius) {
//		particle->addForce(force);
//	}
//}